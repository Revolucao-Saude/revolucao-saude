import { makeStyles } from "@material-ui/core";

const styles = makeStyles({
    textCenter: {
        textAlign: 'center',
        fontSize: '20px'
    },
    imagem: {
        display: 'flex',
        justifyContent: 'center'
    },
    w100: {
        width: '100%'
    }
    
});

export { styles };